return {
  {
    "camspiers/luarocks",
    opts = {
      rocks = { "dkjson" },
      -- The path to the LuaRocks executable. If not set, it will be searched in the system PATH.
      -- luarocks_cmd = "luarocks",
      -- The path to the LuaRocks configuration file. If not set, it will use the default configuration.
      -- config_file = nil,
      -- The path to the LuaRocks tree where packages are installed. If not set, it will use the default tree.
      -- rocks_tree = nil,
    },
  },
}
